include ../includes/mixins.pug
doctype html
html(lang="en")
	head
		include ../includes/header.pug

		link(rel="stylesheet" href="/css/index.css")

		link(rel='apple-touch-icon' sizes='180x180' href='/img/apple-touch-icon.png')
		link(rel='icon' type='image/png' sizes='32x32' href='/img/favicon-32x32.png')
		link(rel='icon' type='image/png' sizes='16x16' href='/img/favicon-16x16.png')
		link(rel='manifest' href='/img/site.webmanifest')
		link(rel='mask-icon' href='/img/safari-pinned-tab.svg' color='#5bbad5')
		meta(name='msapplication-TileColor' content='#da532c')
		meta(name='theme-color' content='#ffffff')

	body.bg-dark-10.text-light
	include ../includes/navbar.pug
	.container-sm
		.jumbotron.text-center.bg-transparent.my-5
			h1.display-3 MingBot
			p.lead The best bot made by 
				span.fw-bold.fst-italic.text-decoration-underline Mingull#2901
				i.fas.fa-star.ps-2
			button.btn.btn-dark Add
		//- .text-light
		//- 	each result, index in results
		//- 		if index + 1 == 1
		//- 			.d-flex.justify-content-between.bg-dark-10
		//- 				.d-flex
		//- 					.p-2.leaderboard-rank.rank-gold.round #{index + 1}.
		//- 					img.round.size-50p.p-2(src=guild.members.cache.get(result.memberId).user.displayAvatarURL({dynamic: true}), alt=guild.members.cache.get(result.memberId).user.username)
		//- 					.p-2 #{guild.members.cache.get(result.memberId).user.username}
		//- 				.d-flex
		//- 					.p-2 Level: #{result.memberLevel}
		//- 					.p-2 XP: #{result.memberXP}
		//- 					.p-2 Send: #{result.messageCount}
		.text-light.bg-dark-5.rounded-corners
			each result, index in results
				div(class="d-flex justify-content-between", class=Object.keys(results).length - 1 === index ? '' : 'border-bottom')
					.d-flex.align-items-center.ps-4
						if index + 1 == 1
							.py-2.me-3.rank-gold.badge.rounded-pill #{index + 1}.
						else if index + 1 == 2
							.py-2.me-3.rank-silver.badge.rounded-pill #{index + 1}.
						else if index + 1 == 3
							.py-2.me-3.rank-bronze.badge.rounded-pill #{index + 1}.
						else
							.py-2.me-3.rank-other.badge.rounded-pill #{index + 1}.
						img.round.py-3.px-2.mx-2(height='100', src=guild.members.cache.get(result.memberId).user.displayAvatarURL({dynamic: true}), alt=guild.members.cache.get(result.memberId).user.username)
						.p-2 #{guild.members.cache.get(result.memberId).user.username}
					.d-flex.align-items-center.pe-4
						.px-4.py-2
							p.text-muted Send:
							p.text-center #{result.messageCount}
						.px-4.py-2
							p.text-muted XP:
							p.text-center #{result.memberXP}
						.px-4.py-2
							p.text-muted Level:
							p.text-center #{result.memberLevel}

				//- else
				//- 	.d-flex.justify-content-between
				//- 		.d-flex.align-items-center.ps-4
				//- 			if index + 1 == 1
				//- 				.py-2.me-3.rank-gold.badge.rounded-pill #{index + 1}.
				//- 			else if index + 1 == 2
				//- 				.py-2.me-3.rank-silver.badge.rounded-pill #{index + 1}.
				//- 			else if index + 1 == 3
				//- 				.py-2.me-3.rank-bronze.badge.rounded-pill #{index + 1}.
				//- 			else
				//- 				.py-2.me-3.rank-other.badge.rounded-pill #{index + 1}.
				//- 			img.round.py-3.mx-2(height='100', src=guild.members.cache.get(result.memberId).user.displayAvatarURL({dynamic: true}), alt=guild.members.cache.get(result.memberId).user.username)
				//- 			.p-2 #{guild.members.cache.get(result.memberId).user.username}
				//- 		.d-flex.align-items-center.pe-4
				//- 			.px-4.py-2
				//- 				p.text-muted XP:
				//- 				p.text-center #{result.memberLevel}
				//- 			.px-4.py-2
				//- 				p.text-muted Send:
				//- 				p.text-center #{result.memberXP}
				//- 			.px-4.py-2
				//- 				p.text-muted Level:
				//- 				p.text-center #{result.messageCount}


	//- else if index + 1 == 2
	//- 	.list-group-item.bg-dark-10
	//- 		img(src=guild.members.cache.get(result.memberId).user.displayAvatarURL({dynamic: true}), alt=guild.members.cache.get(result.memberId).user.username)
	//- 		p #[.leaderboard-rank.rank-silver.round #{index + 1}.] #{guild.members.cache.get(result.memberId).user.username}
	//- else if index + 1 == 3
	//- 	.list-group-item.bg-dark-10
	//- 		p #[.leaderboard-rank.rank-bronze.round #{index + 1}.] #{guild.members.cache.get(result.memberId).user.username}
	//- else
	//- 	.list-group-item.bg-dark-10
		p #[.leaderboard-rank.rank-other.round #{index + 1}.] #{guild.members.cache.get(result.memberId).user.username}